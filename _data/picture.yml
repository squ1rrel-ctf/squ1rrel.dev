presets:
  post_card_image_lazy:
    formats: [webp]
    markup: data_auto
    widths: [300, 600, 900]
    fallback_width: 600
    attributes:
      img: 'class="post-card-image lazyload"'
  author_profile_image:
    formats: [webp]
    widths: [100]
    fallback_width: 100
    attributes:
      img: 'class="author-profile-image"'
